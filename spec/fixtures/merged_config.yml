blocks:
  quote:
    fields:
      text:
        type: string
        required: true
  text:
    fields:
      text:
        type: string
        required: true

namespaces:
  default:
    no_group:
      - text
  blog:
    no_group:
      - text
      - quote